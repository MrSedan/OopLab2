# Commands

Add canonical reusable agent commands here.

Expected structure:

```
.agents/commands/<command-name>.md
```

These files are the source of truth. `agents sync` can render them into native agent surfaces such as:
- Claude skills
- Cursor skills
- Copilot agent files
- OpenCode commands
